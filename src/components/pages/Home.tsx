import React from 'react';
import Hero from '../sections/hero';
import Events from '../sections/Events';

const Home: React.FC = () => {
  return (
    <main>
      <Hero />
      <Events />
    </main>
  );
};

export default Home;
